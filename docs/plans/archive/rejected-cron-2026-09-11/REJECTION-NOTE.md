# Rejected: cron-generated refactor plans for `InstallPromptService.cs` (2026-09-04..09-11)

**Verdict:** `NEEDS-REVISION` -> **ARCHIVED, NOT IMPLEMENTED** (2026-09-13).

These 8 plans were auto-generated by `cron_refactor_planner.py` targeting
`src/Mathilda/Services/InstallPromptService.cs` (193 lines, the largest service
file). The latest review (`2026-09-11T18:47:15` in
`2026-09-11-bigfile-Mathilda-InstallPromptService.cs.md`) returned
`NEEDS-REVISION` with three blocking gaps; earlier ticks (09-04..09-10) returned
`READY-WITH-WARNINGS` but were never acted on. All are now archived together.

## Why not implemented

1. **No extraction objectives.** The plan claims to split `InstallPromptService.cs`
   into smaller modules, but zero of the 12 objectives actually extract code, define
   new modules, or specify what gets split. OBJ-003-OBJ-012 are identical
   "Hardening pass N" boilerplate targeting the *same* file post-extraction --
   there is nothing to extract.
2. **Toolchain mismatch.** The target is a C# file (`.cs`), but OBJ-001 calls for
   `pnpm dlx knip` / `ts-prune` and `rg -n '^import'` -- all TypeScript tooling.
   These do not apply to C# source. The structural analysis section reports
   "(no structural signals detected)" yet the plan header claims objectives are
   "derived from structural analysis."
3. **No DoD / security / why sections.** The DoD checklist promises entries "filled
   in by the IMPLEMENT pass" but none exist. The plan is a skeleton with filler
   objectives, not a reviewable refactor plan.

## Ground truth (verified 2026-09-13)

The file itself is already clean -- the refactor the cron job wanted is done:

- `InstallPromptService.cs` is `IDisposable`, registers a permanent typed JS callback
  bridge (`mathilda.pwa.registerCallbacks`), and has **zero** `eval` strings
  (grep-confirmed). This closes the OBJ-07 de-leak from the 2026-08-18 plan.
- `_dotNetRef` is disposed in `Dispose()` (line 188-190).
- `dotnet build -c Release` -> 0W/0E; `dotnet test -c Release` -> 43/43 passed.
- No `knip`/`ts-prune` needed -- this is a C# project; the equivalent is
  `dotnet build` warnings (0) + unused-`using` analysis, which is clean.

## Recommendation

Do not re-dispatch. If `InstallPromptService.cs` is ever genuinely split, author
a real plan with line-anchored extraction targets (which methods go to which new
file), a C#-appropriate verification gate, and a filled-in DoD per objective.
